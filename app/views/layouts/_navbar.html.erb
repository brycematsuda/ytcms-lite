  <div class="navbar-header">
  <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1"> <span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span></button> <a class="navbar-brand" href="/">YouTube CMS Lite</a>
 </div>
 <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
  <% shows ||= [] %>
  <ul class="nav navbar-nav">
    <% shows.each do |show| %>
      <li class="dropdown">
        <% if !show.seasons.empty? %>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= show.name %><strong class="caret"></strong></a>
          <ul class="dropdown-menu" role="menu">
            <% show.seasons.visible.sorted.each do |season| %>
              <% if !season.episodes.empty? %>
                <li><%= link_to(season.name, {:action => 'show', :permalink => season.permalink}) %></li>
              <% end %>
            <% end %>
          </ul>
          <% end %>
      </li>
    <% end %>
  </ul>
  <ul class="nav navbar-nav navbar-right">
    <li><%= link_to("Admin Panel", :controller => 'shows', :action => 'index') %></li>
  </ul>
</div>