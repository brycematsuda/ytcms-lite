  <div class="navbar-header">
   <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1"> <span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span></button> <a class="navbar-brand" href="/"> <span class="glyphicon glyphicon-flash"></span>&nbsp;YouTube CMS Lite</a>
 </div>
 <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
  <% shows ||= [] %>
  <ul class="nav navbar-nav">
    <li class="dropdown">
      <% shows.each do |show| %>
        <% if !show.seasons.empty? %>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= show.name %><strong class="caret"></strong></a>
          <ul class="dropdown-menu">
            <% show.seasons.visible.sorted.each do |season| %>
              <% if !season.episodes.empty? %>
              <li><%= link_to(season.name, {:action => 'show', :permalink => season.permalink}, :class => current_page?(:action => 'show', :permalink => season.permalink) ? "current" : "") %> </li>
              <% end %>
            <% end %>
          </ul>
        </li>
      <% end %>
    <% end %>
  </ul>
  <ul class="nav navbar-nav navbar-right">
        <li><%= link_to("Admin Panel", :controller => 'admin') %></li>
  </ul>
</div>